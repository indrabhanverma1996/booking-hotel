"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isSameDate = exports.getFirstDayOfWeek = exports.solarMonthDays = exports.isLeapYear = void 0;

/**
 * Determine whether it is a leap year
 * @param {Number} year
 */
var isLeapYear = function isLeapYear(year) {
  if (year % 4 === 0 && year % 100 !== 0) return true;else if (year % 400 === 0) return true;
  return false;
};
/**
 * Get the sun moon, commonly known as the solar calendar month
 * By calculating the second month of the leap year, maybe `29` days
 * @param {Number} month
 * @param {Number} year
 */


exports.isLeapYear = isLeapYear;

var solarMonthDays = function solarMonthDays(year, month) {
  var day = year && isLeapYear(year) ? 29 : 28;
  return [31, day, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][month - 1];
};
/**
 * The first day of the month is the day of the week.
 * @param {Date} date
 */


exports.solarMonthDays = solarMonthDays;

var getFirstDayOfWeek = function getFirstDayOfWeek(year, month) {
  return new Date("".concat(year, "/").concat(month, "/", 1)).getDay();
};
/**
 * Check if a date is the same as another date.
 * Use `Date.prototype.toISOString()` and strict equality checking (`===`) to check
 * if the first date is the same as the second one.
 * @param {Date} dateA
 * @param {Date} dateB
 */


exports.getFirstDayOfWeek = getFirstDayOfWeek;

var isSameDate = function isSameDate(a, b) {
  return a.toISOString() === b.toISOString();
};

exports.isSameDate = isSameDate; 
//# sourceMappingURL=date.js.map