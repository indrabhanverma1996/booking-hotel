/**
 * Determine whether it is a leap year
 * @param {Number} year
 */
export declare const isLeapYear: (year: number) => boolean;
/**
 * Get the sun moon, commonly known as the solar calendar month
 * By calculating the second month of the leap year, maybe `29` days
 * @param {Number} month
 * @param {Number} year
 */
export declare const solarMonthDays: (year: number, month: number) => number;
/**
 * The first day of the month is the day of the week.
 * @param {Date} date
 */
export declare const getFirstDayOfWeek: (year: number, month: number) => number;
/**
 * Check if a date is the same as another date.
 * Use `Date.prototype.toISOString()` and strict equality checking (`===`) to check
 * if the first date is the same as the second one.
 * @param {Date} dateA
 * @param {Date} dateB
 */
export declare const isSameDate: (a: Date, b: Date) => boolean;
